"""Ranking baselines for TilikKlaim.

This package proposes a queue position. It never produces a reason, never states a finding, and
never decides anything — `apps/backend` owns the rules, the reasons, and the human disposition.
"""
